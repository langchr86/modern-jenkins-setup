---

jenkins_app_folder: "/etc/jenkins"
jenkins_config_folder: "{{ jenkins_app_folder }}/jenkins_jobs"
jenkins_docker_folder: "{{ jenkins_app_folder }}/docker"
jenkins_caddy_folder: "{{ jenkins_app_folder }}/caddy"
